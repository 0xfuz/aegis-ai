# URL the browser uses to reach the backend API.
# In docker-compose this is the host-mapped port; server-to-server calls
# inside the compose network would instead use http://backend:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
