import { ComingSoon } from "@/components/layout/coming-soon";

export default function DetectionsPage() {
  return <ComingSoon title="Detections" phase="MITRE mapping and rule suggestions land in a later build phase." />;
}
